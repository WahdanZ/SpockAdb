# Created by .ignore support plugin (hsz.mobi)
/.idea/
/build/
/.gradle/
/gradle.properties
/out/*
/.claude/*
/.intellijPlatform/*
/.kotlin/sessions/*

# macOS
.DS_Store
**/.DS_Store

# Plugin Verifier writes a timestamped scratch directory per verified IDE into the
# project root on every `verifyPlugin` run.
verification-*/
